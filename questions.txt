1. How to open a new lead through new lead id if a lead is already closed?
make a dashboard so the information is shown to the people, an always running sqlite server that handles manual user queries.
2. if no requirements, never treat it as a json object or Lead.
